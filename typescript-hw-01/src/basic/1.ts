const age: number = 50;
const username: string = 'Max';
const toggle: boolean = true;
const empty: null = null;
// * якщо я обира що "а" це число, бо це видно з того що я повертаю в ретьорні:
const callback = (a: number): number => {
  return 100 + a;
};
